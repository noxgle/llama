# Qwen3.6 35B-A3B MTP - Speculative decoding (upstream PR #22673)

# Target model (MTP head embedded in same GGUF)
MODEL=unsloth/Qwen3.6-35B-A3B-MTP-GGUF:UD-Q4_K_M

# Server
PORT=8089
HOST=0.0.0.0

# Context (143K — q8_0/q8_0 KV cache needs ~93% VRAM, 160K causes OOM with MTP)
CTX=143360

# Disable server-side token cap
N_PREDICT=-1

# GPU layers - offload as many as possible, rest in RAM
NGLAYERS=999

# MoE experts on CPU (MoE model, most experts stay in RAM)
CPUMOE=exps=CPU

# Flash Attention
FLASHATTN=on

# Runtime
BATCH=3072
UBATCH=1536
THREADS=4
THREADS_BATCH=4
PARALLEL=1
POLL=50

# Memory
MMAP_FLAG=--no-mmap

# KV cache quantization
CACHE_TYPE_K=q8_0
CACHE_TYPE_V=q8_0

# KV context checkpoints (fewer = less RAM, more reprocessing)
CTX_CHECKPOINTS=4

# MTP speculative decoding
SPEC_TYPE=draft-mtp
SPEC_DRAFT_N_MAX=1
